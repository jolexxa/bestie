name: provider_openrouter
description: >-
  The OpenRouter hosted provider for bestie, built on openrouter_sdk.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  inference_protocol:
    path: ../../infra/inference_protocol
  intentions: ^0.1.0
  openrouter_sdk: ^0.0.8
  provider_protocol:
    path: ../../infra/provider_protocol

dev_dependencies:
  coverage: ^1.9.0
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^10.0.0
