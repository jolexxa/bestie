name: bestie_config
description: Bestie configuration file I/O and schema.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  config_protocol:
    path: ../../infra/config_protocol
  intentions: ^0.1.0

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.0.0
