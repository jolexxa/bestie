name: bestie_platform_windows
description: Windows platform implementation for Bestie.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  bestie_platform_abstractions:
    path: ../bestie_platform_abstractions
  file: ^7.0.1
  intentions: ^0.1.0
  path: ^1.9.1
  platform: ^3.1.5
  win32_dart:
    path: ../../ffi/win32_dart

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.26.3
  very_good_analysis: ^10.0.0
