name: wikimedia_client
description: Wikipedia topic search for Bestie.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  http: ^1.1.0
  intentions: ^0.1.0
  wikimedia_commons_search: ^1.0.0

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.0.0
