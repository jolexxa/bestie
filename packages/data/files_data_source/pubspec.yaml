name: files_data_source
description: Reading, writing, and windowing files on a filesystem.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  byte_codec:
    path: ../../infra/byte_codec
  file: ^7.0.1
  intentions: ^0.1.0
  meta: ^1.18.0
  path: ^1.9.1
  tool_protocol:
    path: ../../infra/tool_protocol

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.26.3
  very_good_analysis: ^10.0.0
