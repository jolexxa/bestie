name: tool_protocol
description: Tool protocol and execution primitives for Bestie agents.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  clock: ^1.1.1
  dart_mappable: ^4.3.0
  meta: ^1.18.0

dev_dependencies:
  build_runner: ^2.4.12
  dart_mappable_builder: ^4.3.0
  fake_async: ^1.3.1
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.2.0
