name: vt_parser
description: >-
  Paul Williams VT500 state-machine parser for ANSI/VT escape sequences,
  used by the agentic_terminal stack.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  coverage: ^1.9.0
  posix_spawner:
    path: ../../ffi/posix_spawner
  process_host:
    path: ../process_host
  process_host_posix:
    path: ../process_host_posix
  test: ^1.25.0
  very_good_analysis: ^10.0.0
