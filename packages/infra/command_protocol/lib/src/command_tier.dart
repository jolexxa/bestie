/// How prominently the palette presents a command.
enum CommandTier {
  /// A key action the palette colors and lists first.
  primary,

  /// An ordinary command listed in contribution order.
  normal,
}
