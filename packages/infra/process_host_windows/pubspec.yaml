name: process_host_windows
description: >-
  The Windows (ConPTY) implementation of bestie's `process_host` contract.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.15.0
  process_host:
    path: ../process_host
  win32_dart:
    path: ../../ffi/win32_dart

dev_dependencies:
  coverage: ^1.9.0
  fake_async: ^1.3.1
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^10.0.0
