name: math_render
description: Renders LaTeX math to a terminal character grid via katex_dart's box tree.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  characters: ^1.3.0
  katex_dart: ^0.1.1
  meta: ^1.3.0

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.0.0
