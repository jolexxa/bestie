name: byte_codec
description: A reusable byte writer and a positioned reader, for binary formats.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dev_dependencies:
  test: ^1.31.1
  very_good_analysis: ^10.2.0
