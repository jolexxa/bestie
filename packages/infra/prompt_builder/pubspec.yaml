name: prompt_builder
description: Provider-agnostic ports for the prompt content an agent runtime needs.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.0.0
