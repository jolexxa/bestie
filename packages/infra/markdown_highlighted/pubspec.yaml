name: markdown_highlighted
description: Markdown rendering with syntax-highlighted code blocks and diffs, for nocterm.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  characters: ^1.3.0
  collection: ^1.19.0
  highlighting: ^0.9.0
  markdown: ^7.2.0
  math_render:
    path: ../math_render
  meta: ^1.3.0
  nocterm: ^0.6.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0