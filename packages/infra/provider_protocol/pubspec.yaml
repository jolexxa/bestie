name: provider_protocol
description: >-
  The hosted-provider contract for bestie: which inference endpoints a
  provider exposes plus its account, key, and model catalog APIs.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  dart_mappable: ^4.3.0
  inference_protocol:
    path: ../inference_protocol
  meta: ^1.15.0

dev_dependencies:
  build_runner: ^2.4.12
  coverage: ^1.9.0
  dart_mappable_builder: ^4.3.0
  test: ^1.25.0
  very_good_analysis: ^10.0.0
