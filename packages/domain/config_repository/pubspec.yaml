name: config_repository
description: Centralized config schema, cascade, and persistence for Bestie.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  bestie_config:
    path: ../../data/bestie_config
  config_protocol:
    path: ../../infra/config_protocol
  intentions: ^0.1.0
  meta: ^1.18.0

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
