/// Identifier for an `AgentSession` within an `AgentRepository`.
typedef AgentSessionId = String;

/// The well-known id of the app's main session.
const AgentSessionId primaryAgentSessionId = 'primary';
