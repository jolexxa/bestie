name: agent_repository
description: Bestie agent session lifecycle repository.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  agent_provider_protocol:
    path: ../../infra/agent_provider_protocol
  clock: ^1.1.1
  dart_mappable: ^4.3.0
  diagnostics:
    path: ../../infra/diagnostics
  file: ^7.0.1
  intentions: ^0.1.0
  logic_blocks: ^1.0.0
  meta: ^1.18.0
  path: ^1.9.1
  prompt_builder:
    path: ../../infra/prompt_builder
  tool_protocol:
    path: ../../infra/tool_protocol
  uuid: ^4.5.3

dev_dependencies:
  build_runner: ^2.4.12
  dart_mappable_builder: ^4.3.0
  fake_async: ^1.3.2
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
