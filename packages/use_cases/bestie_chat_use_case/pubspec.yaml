name: bestie_chat_use_case
description: Chat feature use case — orchestrates the hosted provider and conversation repositories.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  agent_provider_protocol:
    path: ../../infra/agent_provider_protocol
  agent_repository:
    path: ../../domain/agent_repository
  command_protocol:
    path: ../../infra/command_protocol
  config_protocol:
    path: ../../infra/config_protocol
  config_repository:
    path: ../../domain/config_repository
  intentions: ^0.1.0
  logic_blocks: ^1.0.0
  meta: ^1.18.0
  provider_repository:
    path: ../../domain/provider_repository
  tool_protocol:
    path: ../../infra/tool_protocol
dev_dependencies:
  mocktail: ^1.0.4
  provider_protocol:
    path: ../../infra/provider_protocol
  test: ^1.26.3
  very_good_analysis: ^10.0.0
