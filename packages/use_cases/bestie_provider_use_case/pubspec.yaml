name: bestie_provider_use_case
description: Hosted provider use case — settings, credits, model selection, and reconnects.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  agent_provider_protocol:
    path: ../../infra/agent_provider_protocol
  agent_repository:
    path: ../../domain/agent_repository
  command_protocol:
    path: ../../infra/command_protocol
  config_protocol:
    path: ../../infra/config_protocol
  config_repository:
    path: ../../domain/config_repository
  inference_protocol:
    path: ../../infra/inference_protocol
  intentions: ^0.1.0
  meta: ^1.18.0
  provider_protocol:
    path: ../../infra/provider_protocol
  provider_repository:
    path: ../../domain/provider_repository

dev_dependencies:
  coverage: ^1.9.0
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
