name: app_shell_use_case
description: >-
  App shell use case: owns modal overlay visibility (config, palette) and
  contributes the palette command that opens the config screen.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  command_protocol:
    path: ../../infra/command_protocol
  intentions: ^0.1.0

dev_dependencies:
  test: ^1.26.3
  very_good_analysis: ^10.0.0
